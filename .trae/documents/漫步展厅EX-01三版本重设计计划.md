**
必须改动：
- **移除 `transition-all`**  
  用 `transition-colors`、`transition-transform` 替代。
- **补充 `focus-visible`**  
  对每个 `Link` 添加 `focus-visible:outline` 样式（参考 Lightbox/Team 页面已使用的样式）。
- **装饰 icon** 添加 `aria-hidden="true"`  
  `ArrowRight` 作为装饰 icon 时补 `aria-hidden`.

示例：
```tsx
<Link className="... focus-visible:outline focus-visible:outline-2 focus-visible:outline-brand-accent/40 focus-visible:outline-offset-4">
  ...
  <ArrowRight aria-hidden="true" size={18} className="..." />
</Link>
```

---

## Implementation Steps（执行步骤）
1. 在 `ExhibitionDetail.tsx` 中增加 `variant` 读取逻辑（`?v=A|B|C`）。
2. 新建 `const variantStyles = { A: {...}, B: {...}, C: {...} }`，把容器/标题/导语/列表等 class 抽成变量，按 variant 切换。
3. 替换 `transition-all` 为 `transition-colors` / `transition-transform`。
4. 为 Link 添加 `focus-visible` 样式；为 `ArrowRight` 添加 `aria-hidden`.
5. 导语统一去斜体、颜色深灰、字号+1（与 variant 方案一致）。

---

## Verification（验证步骤）
1. 访问 `/hall/EX-01?v=A` / `?v=B` / `?v=C`  
   - 对比三套边距宽度与整体和谐度。
2. 检查导语：
   - 非斜体、深灰色、字号大一号。
3. 单元列表：
   - 行距更紧但移动端仍可点按。
4. 可访问性：
   - Link 有 `focus-visible`；
   - `ArrowRight` 标注 `aria-hidden`；
   - 无 `transition-all`.
5. `npm run lint && npm run build` 通过。

